<!-- POINTER — not the canonical stage contract. -->

This repo predates the template's CONTEXT.md convention; the stage contract for `02-design` lives in **`stages/02-design/STAGE.md`** in this same folder. Read that file.

Created 2026-07-26 (COO apply, Fix 3) to satisfy the template-1.1.0 requirement that every stage carries a `CONTEXT.md`, without renaming `STAGE.md` and breaking existing references. Sub-sprint contracts under `stages/03-build/*/CONTEXT.md` are unaffected and remain canonical for their sprints.
