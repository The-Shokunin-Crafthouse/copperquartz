<!-- POINTER — not the canonical file. -->

# WORKFLOW.md — pointer

The canonical stage flow + approval gates live **once**, in studio-memory:

- **`../studio-memory/WORKFLOW.md`**

Per workflow-contract v1 §2, invariant docs live only in studio-memory and are **referenced by path, never copied**. This repo's root `CLAUDE.md` adapter points there; existing `WORKFLOW.md` / `../../WORKFLOW.md` references resolve here and redirect.

_Do not edit here. Edit the canonical file._
