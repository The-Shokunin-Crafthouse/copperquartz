<!-- POINTER — not the active adapter. -->

# Claude Code adapter — moved

The active Claude Code adapter for this repo is now at the **repo root**: `../../CLAUDE.md`. Claude Code auto-loads the root `CLAUDE.md` at session start; this nested location was never auto-loaded.

This file is kept only so a stale reference cannot silently resolve to old content. Do not edit here — edit `../../CLAUDE.md`.
